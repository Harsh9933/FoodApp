package com.foodai.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Core surfaces (warm cream palette) ──
val Background          = Color(0xFFF7F5F0)
val Surface             = Color(0xFFFFFFFF)
val SurfaceVariant      = Color(0xFFF2EFEA)
val OnBackground        = Color(0xFF1A1A1A)
val OnSurface           = Color(0xFF1A1A1A)
val OnSurfaceVariant    = Color(0xFF7A7A72)

// ── Primary accent (terracotta) ──
val Primary             = Color(0xFFD38269)
val PrimaryContainer    = Color(0xFFF5DDD4)
val OnPrimary           = Color(0xFFFFFFFF)
val OnPrimaryContainer  = Color(0xFF5C2E1E)

// ── Secondary (sage green) ──
val Secondary           = Color(0xFF8B9A7E)
val SecondaryContainer  = Color(0xFFE8EDE4)
val OnSecondary         = Color(0xFFFFFFFF)
val OnSecondaryContainer = Color(0xFF2D3A25)

// ── Borders ──
val Outline             = Color(0xFFE0DDD7)
val OutlineVariant      = Color(0xFFEBE8E3)

// ── Semantic ──
val Error               = Color(0xFFBA1A1A)
val ErrorContainer      = Color(0xFFFCE4E4)
val CalorieAccent       = Color(0xFFD38269)
val HealthGreen         = Color(0xFF6B8F5A)

// ── Bottom nav ──
val NavItemActive       = Color(0xFFD38269)
val NavItemInactive     = Color(0xFFADADA5)
